# PurifAI Backend — Python Dependencies

# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0

# AI / ML — DeBERTa prompt injection classifier
transformers>=4.45.0
torch>=2.0.0

# PDF / File scanning
pypdf>=4.0.0
python-multipart>=0.0.9

# Utilities (already in stdlib but listed for clarity)
# csv, os, datetime, collections, uuid — stdlib
